{{page-title "TestNew"}}
<h1>TD4</h1>
<ul>
  {{#each @model.contacts as |contact|}}
  <tr>
    <td>{{contact.nom}} {{contact.prenom}} {{contact.mail}} <button type="button" {{action "delete" contact}}>Del</button>
        <LinkTo class="btn btn-primary btn-sm" @route="contact.update" @model={{contact}}>Modifier</LinkTo></td>
  </tr>
  {{/each}}
</ul>
<hr/>
<LinkTo class="btn btn-primary btn-sm" @route="contact.add">Ajouter</LinkTo>
{{#if @model.deletedsCount}}
  <button type="button" {{action "confirmer" @model.deleteds}}>Confirmer</button>
  <button type="button" {{action "cancelDeletion" @model.deleteds}}>Annuler la suppression ({{@model.deletedsCount}})</button>
{{/if}}
{{outlet}}